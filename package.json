{
  "name": "gearup",
  "version": "1.0.0",
  "private": true,
  "description": "GearUp monorepo — Railway runs backend via Dockerfile at repo root",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm ci --prefix backend --omit=dev || npm install --prefix backend --omit=dev"
  },
  "engines": {
    "node": ">=20"
  }
}
