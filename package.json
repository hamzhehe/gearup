{
  "name": "gearup",
  "private": true,
  "workspaces": [
    "frontend-next"
  ],
  "scripts": {
    "build": "npm run build --workspace=frontend-next",
    "vercel-build": "npm run build --workspace=frontend-next"
  }
}
