{
  "name": "gearup",
  "private": true,
  "description": "Monorepo shim for Vercel Next.js detection. App builds from frontend/.",
  "dependencies": {
    "next": "16.1.6",
    "react": "19.1.0",
    "react-dom": "19.1.0"
  },
  "scripts": {
    "vercel-build": "npm --prefix frontend ci && npm --prefix frontend run build"
  }
}
